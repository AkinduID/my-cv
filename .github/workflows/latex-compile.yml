name: Build and Deploy CV

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compile LaTeX document
        uses: xu-cheng/latex-action@v3
        with:
          root_file: main.tex

      - name: Prepare Deployment Files
        run: |
          mkdir public
          # Copy the PDF to the public folder
          cp main.pdf public/akindu_delgahagoda_cv.pdf
          
          # Create a custom HTML viewer page
          cat > public/index.html <<EOF
          <!DOCTYPE html>
          <html lang="en">
          <head>
              <meta charset="UTF-8">
              <meta name="viewport" content="width=device-width, initial-scale=1.0">
              <title>Akindu Delgahagoda - CV</title>
              <style>
                  body { margin: 0; display: flex; flex-direction: column; height: 100vh; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; background-color: #525659; }
                  header { background: #333; color: white; padding: 10px 20px; display: flex; justify-content: space-between; align-items: center; box-shadow: 0 2px 5px rgba(0,0,0,0.2); z-index: 10; }
                  h1 { margin: 0; font-size: 1.1rem; font-weight: normal; }
                  .btn { background-color: #2ea44f; color: white; text-decoration: none; padding: 8px 16px; border-radius: 6px; font-size: 0.9rem; font-weight: 600; transition: background-color 0.2s; }
                  .btn:hover { background-color: #2c974b; }
                  iframe { flex-grow: 1; border: none; width: 100%; height: 100%; display: block; }
              </style>
          </head>
          <body>
              <header>
                  <h1>Akindu Delgahagoda - CV</h1>
                  <a href="akindu_delgahagoda_cv.pdf" download class="btn">Download PDF</a>
              </header>
              <!-- Embed the PDF -->
              <iframe src="akindu_delgahagoda_cv.pdf" type="application/pdf">
                  <p>Your browser does not support PDF embeddings. <a href="akindu_delgahagoda_cv.pdf">Click here to download the PDF.</a></p>
              </iframe>
          </body>
          </html>
          EOF

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
